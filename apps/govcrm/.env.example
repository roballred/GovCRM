# Copy to .env.local (never committed). No Postgres runs on the maintainer
# machine — point at a container or remote database for seed/dev.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/govcrm_dev
AUTH_SECRET=generate-with--openssl-rand--base64-32
